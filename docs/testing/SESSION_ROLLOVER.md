# Продолжение задачи и передача после полного завершения

Решение владельца: 2026-09-21. Проект Platforma.CRM.
Canonical checkout: `C:\Users\user\Desktop\Zani`.
Saved project ID: `local-3368c3df041be97f9549005fc6749ad2`.

## Команда владельца — 2026-09-25

**«Передай работу новому чату»** запускает этот протокол у зарегистрированного
primary. Это явное разрешение создать одного преемника с названием **Platforma.CRM**
в том же сохранённом проекте и той же локальной папке, передать ему подготовленное
сообщение, проверить понимание, передать владение и архивировать только source.
Повторно спрашивать разрешение на эти шаги не нужно. Команда не разрешает новую
продуктовую фазу и не отменяет критерии завершения ниже.

Обсуждение/цитирование команды, просьба настроить механизм, закрытие окна,
compact и ручное нажатие «Архивировать» не запускают передачу. На кнопку архива
нет подписки, фонового наблюдателя или автоматического создания чатов.
Разговоры сопоставляются по проверенным идентификаторам проекта/задач из реестра,
не по одинаковому заголовку. В переходе временно существуют два неархивированных
чата, но только один назначенный владелец; во время проверки оба не пишут продукт.

Передача — исполняемая агентом инструкция с нативными инструментами Codex,
а не новый API, CLI-команда или hook. Доверие к hook для неё не требуется.
Если нативные инструменты недоступны, сохранить checkpoint и сообщить ограничение;
не подменять их правкой внутренних хранилищ приложения.

## Сначала завершить текущую работу

Этот протокол заменяет прежнее правило автоматической передачи при compact.
Близость конца окна и фактическое сжатие не разрешают создавать, переносить или
архивировать задачи. Восстановить checkpoint, сверить Git и продолжить текущую
согласованную работу в том же чате. Сжатие движка не отключать.

Ротация допустима только после полного исходного DoD, обязательных checks/review,
согласованной публикации и фактического CI (когда входят в DoD), отсутствия
незавершённых собственных операций и проверки понимания преемника. Не дробить
задним числом согласованную задачу ради формально готового подпункта.
FAILED/BLOCKED/PENDING/unknown означают незавершённость: сохранить checkpoint,
сообщить blocker, соблюдать лимиты попыток, отложить передачу. Не создавать новую
задачу ради обхода blocker. Завершение governance не закрывает иной открытый пакет.

Начинать с [AGENTS.md](../../AGENTS.md),
[handoff](../../actual_docs/PROJECT_HANDOFF.md), [PRIMARY-SESSION](task-state/PRIMARY-SESSION.md)
и `.codex/project-session.json`. Реестр хранит участников/поколение/переход;
checkpoint — разрешённый scope, DoD, проверяемые доказательства и следующий шаг.
Флаги реестра и слово READY не заменяют evidence. Новый продуктовый scope не
возникает от передачи. Явный stop/read-only владельца имеет приоритет.

## Checkpoint без потери прогресса

После смыслового этапа, до паузы или известного завершения записывать в существующую
карточку, не создавая новый backlog:

- текущий разрешённый scope/DoD и non-goals, выполненное/незавершённое/не начатое;
- root, branch, HEAD, task-owned и чужой diff, commands/results/environment;
- failures, проверенные гипотезы и лимиты попыток, собственные незавершённые операции;
- принятые решения отдельно от предложений, один следующий шаг и условия остановки.

Не хранить секреты или полный чат. При strict read-only передать checkpoint в
ответе без записи файлов. Сжатие не требует заморозки текущего primary: после
проверки ownership/Git он продолжает тот же незавершённый scope, не повторяя
закрытое и не сбрасывая попытки. Реальный drift/конфликт владельцев требует
разрешения независимо от compact. Никаких новых source copies/worktrees.

## Восстановление, если прежний чат недоступен

Это не автоматическая передача и не объявление незавершённой задачи завершённой.
Новый чат может без записи восстановить контекст по AGENTS.md, STATUS.md,
checkpoint, реестру и фактическим Git/процессам. Зафиксировать оставшийся scope,
dirty paths, последние проверки и неудачные попытки, незавершённые операции.

Закрытое окно не доказывает остановку исполнителя или фоновых процессов.
До продуктовых записей необходимо проверить отсутствие конкурирующего писателя.
Если старый primary не может выполнить штатную передачу, представить владельцу
восстановленное состояние и запросить адресное решение о назначении нового
исполнителя именно оставшейся задачи. Уже данное явное решение повторно не
запрашивать; общее «продолжай» не трактовать как разрешение захватить ownership.
При неизвестном состоянии процессов/владельца запись остаётся приостановленной.

Этот раздел разрешает диагностику и подготовку решения, а не самостоятельную
правку registry, создание/архивирование задач, остановку чужих процессов или
обход штатного DoD. Исключительное переназначение и его границы фиксируются
отдельным явным решением владельца. Сохранённые незавершённые проверки и работа
переходят как незавершённые, без обнуления попыток и выдуманного PASS.

## Передача: один писатель, проверяемая последовательность

1. Только зарегистрированный primary может начать передачу. До создания преемника
   проверить полное завершение согласованной работы, checks/review/publication/CI,
   отсутствие незавершённых операций и действующее разрешение на передачу.
   Если хотя бы одно условие pending/failed/blocked/unknown — продолжить или
   сохранить blocker в той же задаче, не передавать её. Просьба настроить механизм
   сама по себе не запускает передачу. Полный CI нужен лишь когда он входит в
   критерии исходной задачи; не назначать его заново для документации.
2. Проверить registry, checkpoint и фактический thread ID. Если переход уже есть,
   восстанавливать только его; не создавать второго преемника. Non-primary и
   retired не начинают новую передачу. Для старой неполной записи нельзя выдумать
   missing evidence: проверить исходные материалы и запросить адресное решение
   владельца, если подтверждения недостаточно. Не захватывать ownership.
3. После DoD обновить STATUS, checkpoint и project handoff: завершённый scope,
   проверки/публикация, ограничения, один следующий шаг, отсутствие изменяющих
   операций; не копировать всю историю и секреты. Сохранить projectId,
   sourceThreadId, исходный generation и ожидаемый следующий generation;
   successorThreadId пока null. Поставить transition=preparing. Source прекращает
   продуктовую запись. Изменения передачи — отдельно обозначенные metadata,
   их dirty-состояние не маскировать как чистый checkout.
4. По явной команде выше нативным list_projects сверить проект из реестра и корень;
   при расхождении остановить создание, не выбирать проект по названию наугад.
   Затем create_thread: target.type=project, проверенный projectId,
   environment.type=local, title=Platforma.CRM, prompt по шаблону ниже.
   Local выбран явно по правилу единственной папки, даже если проект — Git.
   Не fork/worktree/source copy; model/thinking не задавать без просьбы владельца.
   Сразу сохранить возвращённый threadId в successorThreadId. При неизвестном
   результате сначала нативный list_threads/read_thread; не создавать повторно.
   clientThreadId не подставлять вместо threadId. Если successor уже записан,
   проверить его проект/статус и переиспользовать. Не создавать чат «для теста».
5. Преемник выполняет только read-only comprehension: читает стартовые документы,
   называет продукт, завершённый scope/DoD/evidence, остаток вне scope и ограничения,
   сверяет Git/ownership. Никаких installs, product tests, commits или новой фазы.
   Primary получает ответ через wait_threads/read_thread и проверяет его содержание
   по таблице ниже. Комментарий «готов» без доказательств не завершает проверку.
   При пробеле send_message_to_thread просит того же преемника уточнить ответ;
   второго преемника не создавать. Зарегистрированный Оркестратор может проверить
   ответ только в рамках уже разрешённого ему участия.
6. До архивации сохранить результаты comprehension и завершения source DoD в
   checkpoint. Записать `handoff` с sourceThreadId, successorThreadId, новым
   generation, completionStatus=`complete`, sourceDoDVerified=true и
   comprehensionVerified=true. Эти поля ставятся только по проверенным evidence:
   checks, review, publication/readback/реальный CI где требуется и finished operations.
7. Повторно проверить отсутствие писателей. Один раз увеличить generation,
   сделать primaryThreadId равным записанному successorThreadId, добавить source
   в retiredThreadIds и поставить transition=awaiting_archive. `handoff.generation`
   должен совпадать с registry.generation. Оба участника пока не пишут продукт.
   Source после переключения не возвращает себе ownership.
8. Нативным set_thread_archived архивировать только записанный source и получить
   подтверждение. Не менять внутренние SQLite/настройки Codex. Перед архивированием
   информация для завершения уже сохранена: старый чат не обязан выполнять код
   после собственной архивации. Подтверждение может проверить преемник/Оркестратор.

Между шагами 7 и 8 source отправляет преемнику follow-up по шаблону завершения
ниже и сообщает пользователю ссылку/карточку нового чата. Это последнее разрешённое
управление передачей со стороны source, не продуктовая запись. Отправка запускает
проверку преемника, но ещё не разрешает ему release: он ждёт native archive readback.
Порядок важен — после самоархивации source может уже не продолжить исполнение.
После успешного create_thread в пользовательском ответе должна быть нативная
карточка созданной задачи с фактически полученным threadId; не выдумывать ссылку.

## Подготовленное сообщение преемнику

Source заполняет все фигурные placeholders проверенными значениями. Это шаблон
человеческого сообщения для create_thread, не shell-команда. Не отправлять ключи,
полный transcript, сырые логи или незаполненные placeholders.

```text
Ты — проверяемый преемник для проекта Platforma.CRM. Это этап восстановления
контекста, пока только чтение. Название проекта не доказывает право записи.
Проект: {projectId}. Папка: {canonicalRoot}. Source: {sourceThreadId}.
Передача: исходное поколение {generation}, ожидаемое {nextGeneration}.
Последний проверенный коммит: {verifiedCommit}; фактические metadata-изменения: {dirtyScope}.
Завершённая задача: {completedScope}. Доказательства: {evidencePathsAndResults}.
Не начато/не разрешено: {nonGoals}. Следующий шаг: {nextStepAndAuthorization}.

Прочитай AGENTS.md, STATUS.md, актуальный раздел docs/testing/task-state/PRIMARY-SESSION.md,
actual_docs/PROJECT_HANDOFF.md, .codex/project-session.json и
docs/testing/SESSION_ROLLOVER.md; затем только необходимые профильные документы.
Сверь Git, корень, владельца и незавершённые операции. Не повторяй закрытые задачи.
Верни краткое понимание продукта/V1, выполненного результата и его проверок,
ограничений/неизвестного, текущего состояния Git, владельца и следующего шага.
Если локальные логи отсутствуют, укажи это; не выдумывай подтверждение.
До отдельного сообщения о завершении перехода не меняй файлы, реестр или Git,
не запускай установку, тесты/серверы, не создавай и не архивируй задачи.
Даже после передачи не начинай новую продуктовую фазу без её разрешения.
```

| Проверяется в ответе | Критерий |
| --- | --- |
| Продукт и требования | Административная CRM для стоматологии, профильные утверждённые правила найдены |
| Завершённая работа | Scope и доказательства соответствуют checkpoint; реализовано не перепутано с deployment |
| Git и владелец | Фактические root/branch/HEAD/dirty названы; preparing не означает право записи |
| Остаток | Запреты, неизвестные доказательства и следующий шаг отражены без нового scope |
| Готовность к передаче | Нет противоречий с source; пробелы исправлены тем же преемником |

Follow-up после проверенного переключения владельца, перед архивированием source:

```text
Твоё понимание проверено; в реестре сохранён переход {sourceThreadId} →
{successorThreadId}, generation {nextGeneration}, transition awaiting_archive.
Заверши только этот переход по разделу идемпотентного завершения протокола.
Подтверди нативным чтением, что именно source архивирован; до этого не очищай
переход и не пиши продукт. Source может архивироваться после этого сообщения.
При отсутствии подтверждения сохрани freeze и сообщи, что именно не подтверждено;
не создавай ещё один чат и не присваивай себе новые полномочия.
После release обнови сводку/receipt и сообщи результат владельцу. Следующий шаг:
{nextStepAndAuthorization}. Новую продуктовую фазу автоматически не запускай.
```

## Повторный запрос и сбои

| Ситуация | Действие |
| --- | --- |
| Работа/обязательная проверка не завершена | Сохранить checkpoint; source остаётся владельцем, новый чат не создавать |
| preparing без понятного результата create | Найти результат через native list/read; при неоднозначности заморозить, не повторять create |
| successor уже записан | Проверить и использовать его; одинаковое имя другого чата не делает его преемником |
| Понимание неверно | Уточнить у того же преемника, source пока не архивировать |
| Повтор команды при awaiting_archive | Восстановить ровно этот переход, не увеличивать generation повторно |
| Архив не подтверждён/инструмент недоступен | Сохранить freeze и конкретный blocker; не объявлять передачу завершённой |
| Повтор после release | Сначала проверить receipt предыдущего перехода; не создавать цепочку чатов из повторного запроса |

Для archive readback использовать нативный результат, явно подтверждающий архив,
либо list_archived_threads с нужным host и пагинацией до точного sourceThreadId.
Отсутствие source в обычном коротком списке задач не является подтверждением архива.

## Идемпотентное завершение подтверждённого перехода

Завершить release может только записанный pre-validated successor, уже указанный
как primary, либо `orchestratorThreadId` из registry. Это узкая служебная операция,
не новое разрешение на продуктовую запись или передачу другой задачи.

Проверить совпадение project/root, source/target/generation, retired source,
awaiting_archive, полного source DoD и comprehension. Затем нативным readback
убедиться, что именно записанный source архивирован. Ни pending tool response,
ни registry flag, ни обещание архивации не достаточны. До подтверждения сохранять
freeze, сообщить blocker; не возобновлять retired source и не создавать другую задачу.

После подтверждения проверить, что запись не изменилась, записать receipt с ключом
перехода, SHA/evidence и native archive confirmation в существующем checkpoint;
поставить transition=idle, successorThreadId=null, handoff=null. Не увеличивать
generation повторно. Повторный вызов для того же завершённого ключа — read-only
проверка receipt, без нового архива, создания или смены владельца. При stale ключе
не трогать текущую запись. Unknown/non-primary без указанных полномочий не завершают
переход. Retired source остаётся без права записи.

Освобождённый primary получает только уведомление о состоявшейся передаче и ждёт
отдельно разрешённого scope. Не продолжать чужой backlog автоматически. Если
владелец отдельно поручил перенос уведомлений/heartbeat, использовать штатный
automation_update, сохраняя status/расписание/уведомления; не включать paused.
Старые задачи не удалять, пользователю сообщить ссылку и фактический результат.

## Read-only hook и доверие

`.codex/hooks.json` подключает `.codex/continuity-hook.cjs` для SessionStart и
UserPromptSubmit. Compact напоминает restore/recheck/continue; primary не получает
`continue:false`. Guard retired SessionStart сохраняет остановку, non-primary —
read-only, незавершённый transition — freeze. Лишь совпадающий проверенный transition
даёт successor/Оркестратору `HANDOFF_FINALIZATION_ONLY` с обязательной внешней
проверкой evidence/archive readback. Hook не читает transcript, не вызывает сеть,
не пишет файлы/Git и не выполняет archive/create/release сам.

Registry: `compactionPolicy=restore_same_thread`, `handoffPolicy=completed_scope_only`.
Старый autoHandoffEnabled=false оставлен для совместимости; даже устаревшее true
не разрешает compact-ротацию. В idle handoff=null; поля pending передачи — только
проверяемое описание, не автоматическая блокировка или доказательство завершения.

По [официальной документации](https://learn.chatgpt.com/docs/hooks#sessionstart),
SessionStart с source=compact выполняется перед продолжением модели; дополнительный
контекст может напомнить проектный порядок, а continue:false прекращает ход.
Формат событий не задаёт наш DoD/ownership — это решение владельца выше.

Успешные unit/CLI/JSON tests не доказывают запуск hook в приложении. Нужны доверие
к проекту и отдельный review/trust точного определения (CLI `/hooks`). Не обходить
trust флагами или правками служебных хранилищ. После изменения definition доверие
нужно проверить заново. Пока реальное срабатывание не наблюдалось, статус остаётся
REQUIRES_REVIEW_AND_TRUST / runtime не проверен. Без hook применять те же правила
continuity вручную, не останавливать активную задачу из-за отсутствия автоматизации.
