# Copy this template to .env.chromatic.local (Git-ignored).
# Never put a real token in this example, chat, VITE_* or command arguments.
CHROMATIC_PROJECT_TOKEN=
# Set synthetic-only only after approving publication of synthetic UI examples.
ZANI_ALLOW_VISUAL_UPLOAD=
